# Supabase 配置 (必需)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# 应用配置 (必需)
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# API 配置
NEXT_PUBLIC_API_URL=http://localhost:3001

# 端口配置
ZFLOW_PORT=3001
ZMEMORY_PORT=3002

# 生产环境安全配置 (生产环境必需)
# PRODUCTION_FRONTEND_URL=https://your-frontend-domain.com
# ALLOWED_ORIGINS=https://your-domain.com,https://your-other-domain.com

# 数据库配置 (可选)
# DATABASE_URL=postgresql://username:password@localhost:5432/zephyros

# 认证配置 (可选)
# NEXTAUTH_URL=http://localhost:3000
# NEXTAUTH_SECRET=your-long-random-secret-string

# Google OAuth 配置 (可选)
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# 监控配置 (生产环境推荐)
# SENTRY_DSN=your-sentry-dsn
# ANALYTICS_ID=your-analytics-id

# AI 代理配置 (agents 功能需要)
# OPENAI_API_KEY=sk-your-openai-api-key
# ANTHROPIC_API_KEY=your-anthropic-api-key

# API Key 加密配置 (用户 API 密钥存储需要)
# 生成方法: openssl rand -hex 32
API_KEY_ENCRYPTION_SECRET=your-32-character-encryption-secret

# Redis 配置 (agents 功能需要)
# REDIS_URL=redis://localhost:6379

# 安全配置 (可选)
# RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
# RATE_LIMIT_MAX_REQUESTS=100
# ENABLE_REQUEST_LOGGING=true

# ================================
# 安全注意事项:
# 1. 生产环境时请使用强密码和随机生成的密钥
# 2. 永远不要将 .env.local 文件提交到版本控制
# 3. 定期轮换 API 密钥和服务密钥
# 4. 确保 CORS 配置正确 (不要在生产环境使用 *)
# ================================ 